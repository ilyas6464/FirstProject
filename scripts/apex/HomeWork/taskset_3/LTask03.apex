/**
Create an Integer variable called startingNumber
Create a loop to countdown from starting number till 0.
 */


for(Integer startingNumber =10; startingNumber >= 0;startingNumber-- ){
     System.debug(startingNumber);
 }